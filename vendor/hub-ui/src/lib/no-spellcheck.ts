/** Free-text fields — never browser spell/grammar underline (VI + technical copy). */
export const HUB_NO_SPELLCHECK_PROPS = {
  spellCheck: false,
  autoCorrect: "off",
  autoCapitalize: "off",
} as const;
