/** Directory table timestamp wrapper — typography SSOT (Date V2 lock). */
export const HUB_DIRECTORY_TIMESTAMP_CLASS = "hub-directory-timestamp";

/** Directory table log column — glyph + label row SSOT (gap/icon parity with column header). */
export const HUB_DIRECTORY_LOG_CLASS = "hub-directory-log-cell";

/** Directory table log note text — pairs with {@link HUB_DIRECTORY_LOG_CLASS}. */
export const HUB_DIRECTORY_LOG_NOTE_CLASS = "hub-directory-log-cell__note";
